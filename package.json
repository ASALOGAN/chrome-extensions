{
  "name": "chrome-extensions",
  "version": "1.0.0",
  "private": true,
  "description": "A collection of Chrome Extensions built with modern web technologies.",
  "workspaces": [
    "extensions/*",
    "shared/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.0.0"
  }
}
